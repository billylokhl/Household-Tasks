# Food AI Architecture & Workflow

## System Architecture

```
┌─────────────────────────────────────────────────────────┐
│                  Google Sheets UI                       │
│         Sheet: "Food Expiration"                        │
│  ┌─────────┬─────────┬─────────┬──────────┬──────────┐ │
│  │ Col A   │ Col B   │ Col C   │ Col D    │ Col E    │ │
│  │ Food    │ ...     │ ...     │ Storage  │ Section  │ │
│  │ Item    │         │         │ Method   │ in Fridge│ │
│  └─────────┴─────────┴─────────┴──────────┴──────────┘ │
│                                                          │
│  Menu: 🍎 Food AI                                       │
│  ├─ Predict Selected Items                              │
│  ├─ Predict All Empty Items                             │
│  ├─ Setup Gemini API                                    │
│  └─ Test API Connection                                 │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ User clicks menu
                     ▼
┌─────────────────────────────────────────────────────────┐
│         Google Apps Script (FoodExpirationAI.js)        │
│                                                          │
│  Main Functions:                                        │
│  ┌────────────────────────────────────────────────┐    │
│  │  predictSelectedFoodStorage()                  │    │
│  │  - Gets selected range from user               │    │
│  │  - Loops through each food item                │    │
│  │  - Calls predictFoodStorage() for each         │    │
│  │  - Writes results to Columns D & E             │    │
│  └────────────────────────────────────────────────┘    │
│                         │                               │
│                         ▼                               │
│  ┌────────────────────────────────────────────────┐    │
│  │  predictFoodStorage(foodItem)                  │    │
│  │  - Builds AI prompt with constraints           │    │
│  │  - Calls Gemini API via UrlFetchApp            │    │
│  │  - Parses JSON response                        │    │
│  │  - Validates storage method & section          │    │
│  │  - Returns {storageMethod, fridgeSection}      │    │
│  └────────────────────────────────────────────────┘    │
│                         │                               │
│                         │ HTTP POST                     │
└─────────────────────────┼───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Google Gemini API (Cloud)                  │
│                                                          │
│  Model: gemini-1.5-flash                                │
│  Endpoint: /v1beta/models/gemini-1.5-flash              │
│                                                          │
│  Input: "Predict storage for: Milk"                    │
│  Output: {"storageMethod":"fridge",                     │
│           "fridgeSection":"2nd shelf"}                  │
│                                                          │
│  Features:                                              │
│  - Natural language understanding                       │
│  - Structured JSON output                               │
│  - Food storage expertise                               │
│  - Free tier: 1,500 requests/day                        │
└─────────────────────────────────────────────────────────┘
```

---

## Workflow Diagram

### User Workflow: Predict Selected Items

```
┌─────────────────────────────────────────────────────┐
│ STEP 1: User enters food items in Column A         │
│                                                     │
│  Row  │ Column A (Food Item) │ Column D │ Column E │
│  ─────┼──────────────────────┼──────────┼──────────│
│   2   │ Milk                 │          │          │
│   3   │ Carrots              │          │          │
│   4   │ Ice cream            │          │          │
└─────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│ STEP 2: User selects cells A2:A4                   │
│         (Click and drag to select)                  │
└─────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│ STEP 3: User clicks menu                           │
│         🍎 Food AI → Predict Selected Items        │
└─────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│ STEP 4: System processes each item                 │
│                                                     │
│  ┌──────────────────────────────────────────────┐ │
│  │ Processing: Milk                             │ │
│  │ → Send to Gemini API                         │ │
│  │ → Response: fridge, 2nd shelf                │ │
│  │ → Write to D2, E2                            │ │
│  │ → Wait 500ms (rate limiting)                 │ │
│  └──────────────────────────────────────────────┘ │
│                                                     │
│  ┌──────────────────────────────────────────────┐ │
│  │ Processing: Carrots                          │ │
│  │ → Send to Gemini API                         │ │
│  │ → Response: fridge, veggie drawer            │ │
│  │ → Write to D3, E3                            │ │
│  │ → Wait 500ms                                 │ │
│  └──────────────────────────────────────────────┘ │
│                                                     │
│  ┌──────────────────────────────────────────────┐ │
│  │ Processing: Ice cream                        │ │
│  │ → Send to Gemini API                         │ │
│  │ → Response: freezer, (empty)                 │ │
│  │ → Write to D4, E4                            │ │
│  └──────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│ STEP 5: Results written to sheet                   │
│                                                     │
│  Row  │ Column A   │ Column D      │ Column E      │
│  ─────┼────────────┼───────────────┼───────────────│
│   2   │ Milk       │ fridge        │ 2nd shelf     │
│   3   │ Carrots    │ fridge        │ veggie drawer │
│   4   │ Ice cream  │ freezer       │               │
└─────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│ STEP 6: Success dialog shown                       │
│                                                     │
│  ┌───────────────────────────────────────────────┐ │
│  │ Predictions complete!                         │ │
│  │                                               │ │
│  │ Success: 3                                    │ │
│  │ Errors: 0                                     │ │
│  │                                               │ │
│  │              [OK]                             │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

---

## AI Prompt Structure

The system sends this type of prompt to Gemini:

```
You are a food storage expert. Given a food item, predict the best
storage method and location.

Food item: "Milk"

Available storage methods:
- fridge
- freezer
- kitchen cabinet
- food cabinet
- cake stand

If storage method is "fridge", also specify the section from:
- 1st shelf (top shelf)
- 2nd shelf
- 3rd shelf
- 4th shelf
- veggie drawer
- fruit drawer

If storage method is NOT "fridge", the section should be empty.

Respond ONLY with a JSON object in this exact format:
{"storageMethod": "chosen_method", "fridgeSection": "chosen_section_or_empty"}
```

**Gemini Response:**
```json
{
  "storageMethod": "fridge",
  "fridgeSection": "2nd shelf"
}
```

---

## API Communication Flow

```
┌──────────────────┐
│ Apps Script      │
│                  │
│ 1. Build prompt  │
│ 2. Create HTTP   │
│    POST request  │
└────────┬─────────┘
         │
         │ HTTPS POST
         │ Headers: Content-Type: application/json
         │ Body: {contents: [{parts: [{text: "..."}]}]}
         │
         ▼
┌─────────────────────────────────────────────┐
│ Gemini API Endpoint                         │
│                                             │
│ URL: https://generativelanguage.googleapis │
│      .com/v1beta/models/gemini-1.5-flash   │
│      :generateContent?key=YOUR_API_KEY     │
│                                             │
│ 3. Process request                          │
│ 4. Generate response                        │
│ 5. Return JSON                              │
└────────┬────────────────────────────────────┘
         │
         │ HTTPS 200 OK
         │ Body: {candidates: [{content: {parts: [{text: "..."}]}}]}
         │
         ▼
┌──────────────────┐
│ Apps Script      │
│                  │
│ 6. Parse JSON    │
│ 7. Validate      │
│ 8. Return result │
└──────────────────┘
```

---

## Data Validation Logic

```
┌─────────────────────────────────────────────────┐
│ Validation Checks                               │
│                                                 │
│ 1. Storage Method Validation                   │
│    ├─ Must be one of:                          │
│    │  • fridge                                 │
│    │  • freezer                                │
│    │  • kitchen cabinet                        │
│    │  • food cabinet                           │
│    │  • cake stand                             │
│    └─ ❌ Error if invalid                      │
│                                                 │
│ 2. Fridge Section Validation                   │
│    ├─ If storageMethod = "fridge":            │
│    │  Must be one of:                          │
│    │  • 1st shelf                              │
│    │  • 2nd shelf                              │
│    │  • 3rd shelf                              │
│    │  • 4th shelf                              │
│    │  • veggie drawer                          │
│    │  • fruit drawer                           │
│    │                                           │
│    └─ If storageMethod ≠ "fridge":            │
│       Must be empty string                     │
│                                                 │
│ 3. JSON Format Validation                      │
│    ├─ Must parse as valid JSON                 │
│    ├─ Must have "storageMethod" field          │
│    └─ Must have "fridgeSection" field          │
│                                                 │
│ ✅ If all pass → Write to sheet                │
│ ❌ If any fail → Log error, continue to next   │
└─────────────────────────────────────────────────┘
```

---

## Error Handling Strategy

```
┌───────────────────────────────────────────────────┐
│ Error Type                │ Handling Strategy     │
├───────────────────────────┼───────────────────────┤
│ API Key Missing           │ Prompt user to setup  │
│ API Key Invalid           │ Show error, retry     │
│ Network Error             │ Log, continue to next │
│ Rate Limit Exceeded       │ Show error, suggest   │
│                           │ waiting 24 hours      │
│ Invalid JSON Response     │ Log, skip item        │
│ Validation Failure        │ Log, skip item        │
│ Sheet Not Found           │ Alert user, exit      │
│ Empty Selection           │ Skip silently         │
└───────────────────────────┴───────────────────────┘

All errors are:
1. Logged to Apps Script execution log
2. Counted in error summary
3. Shown to user in results dialog
```

---

## Security Model

```
┌─────────────────────────────────────────────────────┐
│                 Security Layers                     │
│                                                     │
│  ┌───────────────────────────────────────────────┐ │
│  │ Layer 1: Google OAuth                         │ │
│  │ - User authorizes script to access sheet      │ │
│  │ - Permissions scope: Spreadsheet + URL fetch  │ │
│  └───────────────────────────────────────────────┘ │
│                      │                              │
│  ┌───────────────────────────────────────────────┐ │
│  │ Layer 2: Script Properties (Encrypted)        │ │
│  │ - API key stored in encrypted properties      │ │
│  │ - Not visible in sheet or to other users      │ │
│  │ - Isolated per Google Cloud project           │ │
│  └───────────────────────────────────────────────┘ │
│                      │                              │
│  ┌───────────────────────────────────────────────┐ │
│  │ Layer 3: HTTPS Communication                  │ │
│  │ - All API calls via HTTPS                     │ │
│  │ - API key in query parameter (not header)     │ │
│  │ - Google's infrastructure security            │ │
│  └───────────────────────────────────────────────┘ │
│                      │                              │
│  ┌───────────────────────────────────────────────┐ │
│  │ Layer 4: Data Minimization                    │ │
│  │ - Only food item names sent to API            │ │
│  │ - No personal data transmitted                │ │
│  │ - No storage of API responses                 │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

---

## Performance Characteristics

| Metric | Value | Notes |
|--------|-------|-------|
| **Single Prediction** | ~1 second | API latency + processing |
| **Batch (10 items)** | ~10 seconds | With 500ms delay between |
| **Batch (100 items)** | ~100 seconds | Sequential processing |
| **Daily Limit (Free)** | 1,500 requests | Resets every 24 hours |
| **API Response Size** | ~200 bytes | JSON response |
| **Memory Usage** | Minimal | Stateless function calls |

---

## Testing Strategy

```
┌─────────────────────────────────────────────────────┐
│ Test Function: testGeminiAPI()                      │
│                                                     │
│ Test Case: "Milk"                                  │
│                                                     │
│  1. Call predictFoodStorage("Milk")                 │
│  2. Expect:                                         │
│     {                                               │
│       storageMethod: "fridge",                      │
│       fridgeSection: "2nd shelf" (or similar)       │
│     }                                               │
│  3. Verify:                                         │
│     ✓ Valid storage method                          │
│     ✓ Valid fridge section                          │
│     ✓ Response time < 3 seconds                     │
│     ✓ No exceptions thrown                          │
│                                                     │
│ Manual Tests:                                       │
│  - Various food types (produce, dairy, frozen)      │
│  - Edge cases (empty strings, special chars)        │
│  - Error scenarios (invalid API key, network off)   │
└─────────────────────────────────────────────────────┘
```
